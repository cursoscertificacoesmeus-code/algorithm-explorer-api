# ------------------------------------
# Configuração da Conexão com o Banco de Dados MySQL na VM do Azure
# ------------------------------------
spring.datasource.url=jdbc:mysql://4.174.129.224:3306/algoritmosdb?createDatabaseIfNotExist=true&useSSL=false
spring.datasource.username=azureuser
spring.datasource.password=SUA_SENHA_SEGURA_DO_MYSQL

# ------------------------------------
# Configuração do JPA/Hibernate
# ------------------------------------
# 'update': Tenta sincronizar as entidades com as tabelas existentes (bom para dev)
# 'none': Não faz nada no banco (bom para produção)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect